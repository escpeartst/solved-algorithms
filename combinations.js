/**
 * @param {number} n
 * @param {number} k
 * @return {number[][]}
 */
var combine = function(n, k) {

  while (k > 1) {
    k--;
  }
  return n, k;
};

let n = 4, k = 2;

console.log( combine(n, k) );
